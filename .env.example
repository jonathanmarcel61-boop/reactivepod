# Solo hace falta si server.js se expondrá a otros equipos de la red.
HOST=127.0.0.1
PORT=3000
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
REHABPOD_WS_TOKEN=
